# TODO - Status filtering in EmployeeTable

- [ ] Inspect `EmployeeTable.jsx` filter UI and `queryArgs` usage
- [ ] Add `selectedStatus` state
- [ ] Add Status filter dropdown in the filter toolbar (All/Pending/Completed)
- [ ] Include `status` in `queryArgs` (omit when All)
- [ ] Update `clearFilters()` to reset selectedStatus
- [ ] Ensure pagination resets when status changes
- [ ] Save file and run frontend build/lint if available
